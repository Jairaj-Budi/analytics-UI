import { Routes } from '@angular/router';
import { FormulaBuilderComponent } from './formula-builder/formula-builder.component';

export const routes: Routes = [
    {path: 'formulae-builder', component: FormulaBuilderComponent}
];
